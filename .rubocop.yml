plugins:
  - rubocop-rake
  - rubocop-rspec

AllCops:
  TargetRubyVersion: 3.2
  DisplayCopNames: true
  NewCops: enable

RSpec/SpecFilePathFormat:
  Enabled: false

Style/Attr:
  Enabled: false

Style/AccessorGrouping:
  Exclude:
    - 'lib/fortnox/resources/**/*'
    - 'lib/fortnox/struct.rb'
    - 'spec/fortnox/struct_spec.rb'
    - 'spec/fortnox/resource_spec.rb'

Style/Documentation:
  Enabled: false

# Resource is the gem's central class — the error-translation wrappers alone
# account for a third of it. Splitting it to satisfy the metric would spread
# one cohesive seam across files.
Metrics/ClassLength:
  Exclude:
    - 'lib/fortnox/resource.rb'

Style/WordArray:
  EnforcedStyle: brackets

Style/SymbolArray:
  EnforcedStyle: brackets

RSpec/MultipleMemoizedHelpers:
  Max: 6

RSpec/NestedGroups:
  Max: 5
